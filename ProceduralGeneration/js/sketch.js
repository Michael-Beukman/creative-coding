const cDiv = document.getElementById("canvasDiv");
//todo add bad
let flyers = [];
let checkpoints = [];
let population;
function setup() {
  let canvas = createCanvas(windowWidth, windowHeight);
  canvas.parent(cDiv);

}

function draw() {
  background(0);
  
}
